
function decode_contact () {
  let xyz = Qid('xyz')
  let mia_nomo = B.innerHTML.match(/="https:\/\/github[.]com\/([a-z0-9]+)\//)[1]
  const zyx = mia_nomo + String.fromCharCode(1<<6) + 'pro' + (''+(!![]))[+![]] + 'moc.liamno'.split('').reverse().join('')
  xyz.href = zyx.slice(16,20) + 'to' + String.fromCharCode('xyz'.charCodeAt(1<<1)^0O100) + zyx
  // if you know a better way, please let me know!
}

clear_board()
init_inputs()
init_dark()    // follow system preferences, or user-overriding
change_qari()  // to take effect, b/c inputs usually keep their value on refresh
decode_contact()
ligilumi()
